{% extends "base.twig" %} {% block content %} {# {{ post.thumbnail.src }} #}
<div class="content-wrapper">
  <div class="post-type-{{ post.post_type }}" id="post-{{ post.ID }}">
    {{ post.content }}
  </div>
</div>
<!-- /content-wrapper -->
{% endblock %}
